# Bundled subagent library

Agent definition markdown files sourced from [agency-agents](https://github.com/agency-agents/agency-agents).

To refresh from a local checkout:

```bash
AGENCY_AGENTS_ROOT=/path/to/agency-agents ./scripts/sync-subagent-library.sh
```
