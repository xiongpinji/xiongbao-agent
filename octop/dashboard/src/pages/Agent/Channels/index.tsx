/**
 * Agent channels UI (Personalization / Experts embed ChannelsPanel).
 * Route `/channels` redirects to `/personalization/channels`.
 */
export { default as ChannelsPanel } from "./ChannelsPanel";
export { default } from "./ChannelsPanel";
