{
  "name": "xiongbao-mobile",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Mobile / PWA shell for 熊宝 Agent. Talks directly to Octop backend over HTTPS + WebSocket.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-transform-types --test src/api/chatSocket.test.mjs",
    "e2e": "node e2e/run-smoke.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.5",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.6"
  }
}
