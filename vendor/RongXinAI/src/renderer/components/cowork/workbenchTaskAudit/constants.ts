export const WorkbenchTaskRunFilter = {
  All: 'all',
} as const;

/** Shared expo-out easing for every timeline animation so the page moves as one voice. */
export const TIMELINE_EASE: [number, number, number, number] = [0.16, 1, 0.3, 1];
